[saga.adaptors.condor_job.cli.environment]
# A slightly less sanitized environment for condor-demo

  SAGA_LOCATION     = $[saga.location]

# This should cover shared libraries on all platforms
  DYLD_LIBRARY_PATH = ${DYLD_LIBRARY_PATH}
  LD_LIBRARY_PATH   = ${LD_LIBRARY_PATH}
  PATH              = ${PATH}

